{
  "name": "@bharatdcim/api",
  "version": "0.1.0",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "wrangler dev src/index.ts",
    "build": "tsc --noEmit",
    "test": "vitest run",
    "lint": "tsc --noEmit",
    "db:generate": "drizzle-kit generate",
    "db:push": "drizzle-kit push",
    "db:migrate": "tsx src/db/migrate.ts"
  },
  "dependencies": {
    "@bharatdcim/billing-engine": "workspace:*",
    "@hono/swagger-ui": "^0.5.3",
    "@libsql/client": "^0.14",
    "drizzle-orm": "^0.38",
    "hono": "^4.7",
    "jose": "^6"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.0",
    "drizzle-kit": "^0.30",
    "typescript": "^5.7",
    "vitest": "^3.0",
    "wrangler": "^4.0"
  }
}
